import { Dialog } from '@mui/material'
import React from 'react'

function DialogInformation({ open, handleClose, title, content, handleDelete }) {
  return (
    <Dialog>
        
    </Dialog>
  )
}

export default DialogInformation